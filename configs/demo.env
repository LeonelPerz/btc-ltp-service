# Demo de Precedencia - Variables de Entorno
# Estas variables sobrescriben la configuración YAML

# ============================================
# PRECEDENCIA: defaults → YAML → ENV vars
# ============================================

# Servidor - Sobrescribe config.demo-precedence.yaml (port: 9000)
PORT=8080
SHUTDOWN_TIMEOUT=30s

# Cache - Sobrescribe configuración YAML
CACHE_BACKEND=memory         # Sobrescribe "redis" del YAML
CACHE_TTL=30s               # Sobrescribe "60s" del YAML

# Redis - Sobrescribe configuración YAML
REDIS_ADDR=localhost:6379   # Sobrescribe "redis:6379" del YAML  
REDIS_PASSWORD=             # Sobrescribe "secret" del YAML
REDIS_DB=0                  # Sobrescribe "1" del YAML

# Rate Limiting - Sobrescribe configuración YAML
RATE_LIMIT_ENABLED=true     # Sobrescribe "false" del YAML
RATE_LIMIT_CAPACITY=100     # Sobrescribe "50" del YAML
RATE_LIMIT_REFILL_RATE=10   # Sobrescribe "5" del YAML

# Logging - Sobrescribe configuración YAML
LOG_LEVEL=info              # Sobrescribe "debug" del YAML
LOG_FORMAT=json             # Sobrescribe "text" del YAML

# Business - Sobrescribe configuración YAML
SUPPORTED_PAIRS=BTC/USD,ETH/USD,LTC/USD  # Sobrescribe pares EUR del YAML

# Kraken - Timeouts específicos
KRAKEN_TIMEOUT=10s
KRAKEN_FALLBACK_TIMEOUT=15s

# ============================================
# CASOS DE VALIDACIÓN FAIL-FAST
# ============================================

# Para probar TTL inválido, descomenta:
# CACHE_TTL=50ms              # ❌ Muy corto - debe fallar

# Para probar pares inválidos, descomenta:  
# SUPPORTED_PAIRS=BTC/USD,DOGE/MOON,INVALID  # ❌ Pares desconocidos
