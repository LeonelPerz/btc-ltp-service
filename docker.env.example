# ============================================
# Docker Environment Variables Example
# Copy values to your environment or .env file
# ============================================

# ====================
# Redis Configuration
# ====================
REDIS_PASSWORD=your-redis-password-here

# ====================
# Authentication Configuration (NEW!)
# ====================
# Toggle API key authentication (true/false)
AUTH_ENABLED=false

# Your secret API key (required when AUTH_ENABLED=true)
AUTH_API_KEY=your-super-secret-api-key-2024

# Header name for API key authentication
AUTH_HEADER_NAME=X-API-Key

# ====================
# Logging Configuration
# ====================
LOG_LEVEL=info
LOG_FORMAT=json

# ====================
# Development Flags
# ====================
MOCK_MODE=false
DEBUG_MODE=false  
DEV_MODE=false

# ====================
# Monitoring Configuration (Optional)
# ====================
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin123

# ============================================
# Usage Examples:
# ============================================

# 1. Production without auth:
#    AUTH_ENABLED=false
#    docker-compose up -d

# 2. Production with auth:
#    AUTH_ENABLED=true
#    AUTH_API_KEY=prod-secret-2024
#    docker-compose up -d

# 3. Development mode:
#    docker-compose --profile dev up -d btc-ltp-service-dev

# 4. Full monitoring stack:
#    docker-compose --profile monitoring up -d

# ============================================
# Auth Testing Commands:
# ============================================

# Test without auth (works when AUTH_ENABLED=false):
# curl http://localhost:8080/api/v1/ltp?pair=BTC/USD

# Test with auth (required when AUTH_ENABLED=true):
# curl -H "X-API-Key: your-super-secret-api-key-2024" http://localhost:8080/api/v1/ltp?pair=BTC/USD

# Always works without auth:
# curl http://localhost:8080/ready
# curl http://localhost:8080/health
